# Повторная проверка PLAN PORTAL — 20.09.2026

## Выполненные исправления

- Подключён адаптер Microsoft SQL Server (`pymssql`) для `EQUIPMENT.dbo.EQUIPMENT_STOP`; пустой `DOWNTIME_QUERY` использует встроенный SELECT.
- Линии сопоставляются по `SEQNAME` с учётом цеха `SDEPNAME`, вариантов названий и неоднозначностей. Числа в скобках не принимаются за CSB.
- По явному решению владельца начало — `EQUIP_START_DATE`, конец — `STOP_DATE`. Обратные интервалы и отсутствующее начало показываются в диагностике; даты не переставляются автоматически.
- «Частичный простой» — 50%; «Простой линии» — 80%; «Поломка» и неизвестный вид — без оценки; «Без простоя» исключается. Проценты настраиваются в администрировании.
- Потери не подменяются нулём, если неизвестен тип или нет скорости по заданиям дня. План от чтения простоев не изменяется.
- Переход между разделами возвращает страницу к началу. Поздний ответ предыдущего периода не заменяет выбранный период.
- Исправлена обработка истёкшей сессии: ответ 401 возвращает форму входа.
- Для модальных окон и карточки задания блокируется прокрутка фона.
- Удалена вводящая в заблуждение подпись общей мощности «22 ч/сутки» рядом с фактическим 11-часовым графиком.
- Ошибки валидации API показывают поле и причину вместо `[object Object]`.
- Персональный запрет просмотра плана распространяется также на сводный `/api/dashboard`.
- Параметры BOM передаются из ENV в Compose; порт по умолчанию в настройках приведён к 15500.
- README переписан: назначение, все разделы, расчёты, модель данных, API, роли, конфигурация, интеграции, установка, обновление и восстановление.

## Проверки

- Backend: **76 passed, 1 skipped**. Пропуск — отдельный PostgreSQL-тест, который затем успешно выполнен на VPS в специально созданной тестовой БД: **1 passed**.
- Конкурентные записи в PostgreSQL: ровно одна успешная запись и один ответ 409. Тестовый контейнер удалён после проверки; production-БД в этом тесте не использовалась.
- Frontend: TypeScript и production-сборка Vite проходят.
- Регрессии: авторизация и блокировка demo-механизма в production, персональные права, импорты, расчёт/дробление, ручные даты и приоритеты, справочник/XLSX, АЗ, почтовые ошибки, сохранность seed, интервалы простоев.
- Браузер: тёмная и светлая темы, день/неделя, закрепление панели периода/дат/ползунка, длинная страница, переход в справочник, фильтрация и сохранение фильтра, объединённый справочник линий, ручной календарь, персональные права, складской предпросмотр письма, АЗ без применения, карта цехов и обозначение заглушки факта.
- Проверка проводилась на существующих локальных данных. Изменяющие данные сценарии выполнялись автоматическими тестами в изолированных БД, не импортом поверх production.

## Реальные данные EQUIPMENT

Чтение таблицы с VPS подтверждено. На момент проверки: 5430 записей, из них в 5418 `STOP_DATE` раньше `EQUIP_START_DATE`, в 12 начало отсутствует. Поэтому при согласованном направлении дат эти записи не создают корректных интервалов и не начисляют потери. «Без простоя» исключается ещё до валидации. Количество ошибок на экране зависит от выбранного периода.

Значения `SSTOP_TYPE`: частичный — 1632, простой линии — 3330, поломка — 5, пустое — 454, без простоя — 9. Эти числа — снимок внешней БД, не постоянные настройки.

Неизвестные помещения/оборудование не назначаются случайным линиям. Новые соответствия следует добавлять после подтверждения производственного смысла.

## Границы проверки

- Принятие письма SMTP-сервером не подтверждает доставку во входящие. Реальная рассылка в ходе аудита не выполнялась.
- Факт ERP/CSB остаётся заглушкой; автоматический факт АЗ и реальная HTTP-отправка в CSB не подключены.
- Срок годности требует согласованного API. Внешние BOM/LDAP зависят от корпоративной сети и её настроек.
- Прогон тестов не гарантирует отсутствие всех ошибок; постоянного frontend e2e и нагрузочного набора пока нет.
- Зависимости frontend используют `latest`; установленные образы следует сохранять для воспроизводимого отката.

## Сохранность и выпуск

Перед обновлением создан дамп `/home/docker/plan-portal-backups/pre-downtime-audit-20260920.dump` и сохранены предыдущие образы с тегом `pre-equipment-audit`. После пересоздания backend/frontend SHA-256 всех семи таблиц совпали с исходными: 587 артикулов, 21 запись линии (включая неактивные), 739 связей мощности, 2304 календарные мощности, 894 строки потребности, 1275 заданий, 1 план. Существующие данные сохранены.

Развёрнут код `f0acb88`. При ограниченном доступе VPS к PyPI драйвер установлен из заранее подготовленного Linux wheel, frontend — из проверенной production-сборки. Проверки через Nginx: health 200, режим LDAP, анонимные plan/dashboard 401. В production `local_auth_enabled=false`.

Проверка действующего адаптера за 20.09.2026: `connected`, пять ошибок дат, все пять имён сопоставлены, корректных интервалов нет. Диагностика SMTP успешна без отправки письма. На узком экране 900 px элементы верхней панели переносятся без наложения; после проверки исходный размер браузера восстановлен. В журнале проверяемой вкладки ошибок JavaScript не обнаружено.

Прочие приложения VPS и их базы не входят в изменения. Секреты ENV и локальные тестовые учётные данные не включаются в Git.
